##  Project Structure

```text
Robot-Failure-Prediction-XAI/
│
├── 📁 data/                        # 전처리된 데이터 파일 (Git에는 제외 권장)
│   ├── X_train.npy                 # 학습 데이터 (StandardScaler 적용됨)
│   └── X_test.npy                  # 테스트 데이터
│
├── 📁 models/                      # 학습된 베스트 모델 가중치 (.pth)
│   ├── gru_model_best.pth          # 기본 GRU 모델 (Baseline)
│   └── da_rnn_best_maxpool.pth     # 제안 모델 (DA-RNN)
│
├── 📁 src/                         # 모델 학습 및 정의 코드
│   ├── model_gru.py                # GRU 모델 클래스 정의
│   ├── model_darnn.py              # DA-RNN 모델 클래스 정의
│   ├── train.py                    # 학습 실행 스크립트
│   └── evaluate.py                 # 성능 평가 (Confusion Matrix 등)
│
├── 📁 analysis/                    # [핵심] XAI 분석 및 해석 코드
│   ├── step1_gru_shap.py           # [Step 1] GRU 모델 Global SHAP 분석
│   ├── step2_darnn_shap.py         # [Step 2] DA-RNN 모델 Global SHAP 분석
│   └── step3_darnn_attention.py    # [Step 3] DA-RNN Input Attention 분석
│
├── 📁 results/                     # 분석 결과 시각화 이미지
│   ├── confusion_matrix_final.png  # 모델 성능 비교 (Confusion Matrix)
│   ├── gru_global_shap_bar.png     # GRU 변수 중요도 (Bar Plot)
│   ├── darnn_global_shap_bar.png   # DA-RNN 변수 중요도 (Bar Plot)
│   ├── darnn_global_shap_dot.png   # DA-RNN 변수 영향력 분포 (Dot Plot)
│   └── darnn_global_attention.png  # DA-RNN Attention 분포 (Bar Plot)
│
├── .gitignore                      # Git 업로드 제외 설정 파일
├── requirements.txt                # 필요한 라이브러리 목록
└── README.md                       # 프로젝트 결과 보고서
